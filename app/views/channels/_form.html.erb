<%= form_with(model: channel) do |form| %>
  <% if channel.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(channel.errors.count, "error") %> prohibited this channel from being saved:</h2>

      <ul>
        <% channel.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :discord_id, style: "display: block" %>
    <%= form.number_field :discord_id %>
  </div>

  <div>
    <%= form.label :channel_type, style: "display: block" %>
    <%= form.text_field :channel_type %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
